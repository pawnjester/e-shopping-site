<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/content_padding_normal"
    android:layout_margin="@dimen/content_padding_normal"
    android:orientation="horizontal"
    android:baselineAligned="false">

    <ImageView
        android:contentDescription="@string/icon"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_mtn_mobile_money"
        android:id="@+id/mtn_mobile_money"
        android:layout_weight="1" />

    <ImageView
        android:contentDescription="@string/icon"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_airtel_money_icon_copy"
        android:id="@+id/airtel_money"
        android:layout_weight="1" />

    <ImageView
        android:visibility="gone"
        android:contentDescription="@string/icon"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_tigo_cash"
        android:id="@+id/tigo_cash"
        android:layout_weight="1" />
</LinearLayout>